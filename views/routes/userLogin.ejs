<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen bg-zinc-900">
    <div class="bg-zinc-800 p-8 rounded-xl shadow-lg w-full max-w-md">
        <!-- Logo Section -->
        <div class="text-center mb-6">
            <h2 class="text-3xl font-extrabold text-white">Welcome Back</h2>
            <p class="text-zinc-400">Login to your account</p>
        </div>

        <!-- Form Section -->
        <form action="/login" method="POST" class="space-y-5">
            <!-- Email Input -->
            <div>
                <label for="email" class="block text-sm font-medium text-zinc-400">Email</label>
                <input type="email" id="email" name="email" required placeholder="Enter your email" class="mt-1 block w-full rounded-lg border border-zinc-700 bg-zinc-900 p-3 text-white focus:ring-blue-500 focus:border-blue-500">
            </div>

            <!-- Password Input -->
            <div>
                <label for="password" class="block text-sm font-medium text-zinc-400">Password</label>
                <input type="password" id="password" name="password" required placeholder="Enter your password" class="mt-1 block w-full rounded-lg border border-zinc-700 bg-zinc-900 p-3 text-white focus:ring-blue-500 focus:border-blue-500">
            </div>

            <!-- Remember Me & Forgot Password -->
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <input id="remember" type="checkbox" class="h-4 w-4 text-blue-600 border-zinc-700 bg-zinc-900 rounded focus:ring-blue-500">
                    <label for="remember" class="ml-2 text-sm text-zinc-400">Remember me</label>
                </div>
                <a href="#" class="text-sm text-blue-500 hover:underline">Forgot password?</a>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="w-full bg-green-600 text-white font-semibold py-3 px-4 rounded-lg shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Login
            </button>
        </form>

        <!-- Divider -->
        <div class="relative my-6">
            <div class="absolute inset-0 flex items-center">
                <div class="w-full border-t border-zinc-700"></div>
            </div>
            <div class="relative flex justify-center text-sm">
                <span class="bg-zinc-800 px-2 text-zinc-400">Or continue with</span>
            </div>
        </div>

        <!-- Social Buttons -->
        <div class="grid grid-cols-3 gap-4">
            <button class="flex items-center justify-center bg-zinc-700 hover:bg-zinc-600 rounded-lg py-2 px-4 text-white">
                <img src="https://www.svgrepo.com/show/355037/google.svg" alt="Google" class="h-5 w-5 mr-2"> Google
            </button>
            <button class="flex items-center justify-center bg-zinc-700 hover:bg-zinc-600 rounded-lg py-2 px-4 text-white">
                <img src="https://www.svgrepo.com/show/157818/facebook.svg" alt="Facebook" class="h-5 w-5 mr-2"> Facebook
            </button>
            <button class="flex items-center justify-center bg-zinc-700 hover:bg-zinc-600 rounded-lg py-2 px-4 text-white">
                <img src="https://www.svgrepo.com/show/349524/github.svg" alt="GitHub" class="h-5 w-5 mr-2"> GitHub
            </button>
        </div>

        <!-- Signup Link -->
        <p class="mt-6 text-center text-sm text-zinc-400">
            Don't have an account? 
            <a href="/user/create" class="text-blue-500 font-medium hover:underline">Sign up</a>
        </p>
    </div>
</body>
</html>
